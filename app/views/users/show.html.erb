<%= render 'shared/page_title', title: "User #{@user.first_name}'s Profile" %>

<div class="row">
  <div class= "col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user, size: 150%>
  </div>
</div>

<h4 align="center"><%= @user.username %>'s Projects</h4>

<div align="center">
  <%= will_paginate @user_projects%>
</div>
<%= render 'projects/project', obj: @user_projects %>

<div align="center">
  <%= will_paginate @user_projects%>
</div>